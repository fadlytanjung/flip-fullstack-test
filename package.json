{
  "name": "flip-fullstack-test",
  "version": "1.0.0",
  "description": "Bank Statement Viewer - Full Stack Application",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd backend && make install && cd ../frontend && npm install",
    "dev": "concurrently \"cd backend && air\" \"cd frontend && npm run dev\"",
    "dev:backend": "cd backend && air",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "concurrently \"cd backend && make build\" \"cd frontend && npm run build\"",
    "build:backend": "cd backend && make build",
    "build:frontend": "cd frontend && npm run build",
    "start": "concurrently \"cd backend && make run\" \"cd frontend && npm start\"",
    "start:backend": "cd backend && make run",
    "start:frontend": "cd frontend && npm start",
    "test": "cd backend && make test",
    "coverage": "cd backend && make coverage",
    "lint": "concurrently \"cd backend && make lint\" \"cd frontend && npm run lint\"",
    "lint:backend": "cd backend && make lint",
    "lint:frontend": "cd frontend && npm run lint",
    "format": "cd backend && make format",
    "tidy": "cd backend && make tidy"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
